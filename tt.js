    if(command === 'qwertyuiopasdfghjklzxcvbnm#8138-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Currymuncer#7754-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Heyheyheychocolaterain#2698-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Donavon#1031-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Existential Dread#9976-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Floooopie#3971-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'gang steve#4121-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Goosan#6969-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'GrambleG#0189-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'hassan_#8033-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Homeless Philosopher#9714-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'jeishawn#5030-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'KiriJonash#1694-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'KingNugget#8240-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Kingo1983#0909-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'lucassatay#4052-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Manfirm#3743-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'carrot#8134-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Shlappeh#9828-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'mcaio#2057-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'mathisyum#3026-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Grim#1018-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Mugii#6941-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'nik#4318-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Rasey#4219-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'roller#0001-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Romzy#8124-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Sam_#2509-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'sasuke-irl#7291-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Scooper#9636-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'shanni#1458-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Donut#4670-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Sparkar#7458-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'sugon#5242-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'TEchnogogog#0719-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'technotheking#9747-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'The Putinator#3580-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'TheAverageNick#2757-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === '7in#9444-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'willandra#4651-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Zamboney#7574-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'zenaa#1957-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'ZenonDaBoss#2664-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }
    if(command === 'Mahorny#7050-n-word-times'){
        fsLibrary.readFile('./no_nig_files/no_nig_values.txt', (error, txtString) => {
            if (error) throw err;
            message.channel.send('x person ' + txtString.toString());
        })
    }